import { Link } from "react-router-dom";

const Home = (props) => {
    return (
        <div>
            <h1 style={{ color: "Grey" }}>Welcome</h1>
            {/* <Link to={"/home"}>Go to About </Link> */}
        </div>
    );
}

export default Home;